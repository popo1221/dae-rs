name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    name: Rust Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: "dae-rs"

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run clippy
        run: |
          cargo clippy --all \
            -- -A clippy::should_implement_trait \
            -A clippy::module_inception \
            -A clippy::manual_strip \
            -A clippy::manual_flatten \
            -A clippy::doc_overindented_list_items \
            -A clippy::new_ret_no_self \
            -A clippy::unnecessary_box_returns

      - name: Build
        run: cargo build --all

      - name: Run tests
        run: cargo test --all

  security:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-audit
        run: |
          curl -L https://github.com/rustsec/rustsec/releases/download/cargo-audit%2Fv0.18.3/cargo-audit-x86_64-unknown-linux-gnu.tar.gz | tar xz
          mv cargo-audit /usr/local/bin/

      - name: Run security audit
        run: cargo audit || true
