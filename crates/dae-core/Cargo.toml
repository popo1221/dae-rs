[package]
name = "dae-core"
version = "0.1.0"
edition = "2021"
description = "Core engine for dae-rs"

[dependencies]
tokio = { version = "1", features = ["full"] }
tracing = "0.1"

[dev-dependencies]
tokio-test = "0.4"
