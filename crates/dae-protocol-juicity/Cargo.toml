[package]
name = "dae-protocol-juicity"
version = "0.1.0"
edition = "2021"
description = "Juicity protocol handler for dae-rs"

[dependencies]
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
thiserror = "1"
rand = "0.8"

[dev-dependencies]
tokio-test = "0.4"
