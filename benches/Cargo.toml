[package]
name = "dae-benches"
version = "0.1.0"
edition = "2021"

[dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
tokio = { version = "1", features = ["full"] }
dae-proxy = { path = "../packages/dae-proxy" }

[[bench]]
name = "main"
harness = false
path = "src/main.rs"
