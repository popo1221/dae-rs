[package]
name = "dae-config"
version = "0.1.0"
edition = "2021"
description = "Configuration for dae-rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
ipnet = "2"
thiserror = "1"
